name: Unit Tests

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1
            - uses: borales/actions-yarn@v2.0.0
            - name: Install
              run: yarn install
            - name: Unit Tests
              run: yarn test
